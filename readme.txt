Required Python Interpreter: version 3.7

To install all required packages to run the package, open terminal in your IDE in the project's location, and type:

pip install -r requirements.txt


Must have:
	masks folder with deeplabv3_xception_tf_dim_ordering_tf_kernels.h5 in it
	above requirements

Run program from Main.py file
Two options:
	Video to Filtered Frames
	

Missing: Scale